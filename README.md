Fullstack Auth Example
======================

This package contains a simple full-stack authentication example.

Structure:
- backend/ : Express + MongoDB server
- frontend/: React app (create-react-app-like structure)

Follow READMEs inside backend and frontend to run the apps locally.

Notes:
- Backend expects a MongoDB instance (local or cloud).
- JWT secret should be set in the .env file in backend.
